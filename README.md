# Python Installation Instructions

To install the required packages for Python, use the following command:

```bash
pip install -r requirements.txt
```

## Example Usage

Here's how you can use the Python version of the application:

```python
import wascan

result = wascan.run_scan("your_target")
print(result)
```